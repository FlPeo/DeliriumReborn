package view;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import tools.Path;

public class ClayView extends ImageView {
        public ClayView() {
            super(new Image(Path.clay));
    }
}
