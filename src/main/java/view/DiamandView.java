package view;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import tools.Path;

public class DiamandView extends ImageView {
        public DiamandView() {
            super(new Image(Path.diamond));
    }
}
